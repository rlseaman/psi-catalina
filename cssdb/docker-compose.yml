version: "3"
services:
  proxy:
    image: owasp/modsecurity-crs
    environment:
      PROXY: 1
      PROXYLOCATION: "http://cssdb:8080/"
      ALLOWED_METHODS: "GET HEAD OPTIONS"
    ports:
      - 8086:80
  cssdb:
    build: flask
    volumes:
      - cssdb_data:/data
  dbbuild:
    build: dbbuild
    volumes:
      - cssdb_data:/data
volumes:
  cssdb_data:
